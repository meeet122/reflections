What happens when you initialize a repository? Why do you need to do it?
when I initialize git repo, .git is created where the metadata is stored and then you 
can treat that folder as a repo, we need to do it to commit.

How can you use the staging area to make sure you have one commit per logical change?
you can use git diff to make sure you have one commit per logical change


What are some situations when branches would be helpful in keeping your history 
organized? How would branches help?
branches would help when I need to have different versions of the same project running differently. 
it will help me by doing so easily

How do the diagrams help you visualize the branch structure?
Diagrams helps to visualize the commits and branches so we can figure out if there is any unreachable
commits.




